spring.application.name=api

# --- Database (Neon PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://ep-sweet-frog-afd4x2r6.c-2.us-west-2.aws.neon.tech/neondb?sslmode=require&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_qZ1RGDKwNag2
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Flyway ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.url=jdbc:postgresql://ep-sweet-frog-afd4x2r6.c-2.us-west-2.aws.neon.tech/neondb?sslmode=require&channelBinding=require
spring.flyway.user=neondb_owner
spring.flyway.password=npg_qZ1RGDKwNag2

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# --- JWT ---
jwt.secret=${JWT_SECRET:changeme-replace-with-a-256-bit-secret}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# --- Stripe ---
stripe.secret-key=${STRIPE_SECRET_KEY:sk_test_placeholder}

# --- Claude API ---
claude.api-key=${CLAUDE_API_KEY:placeholder}
